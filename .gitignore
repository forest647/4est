# Old site (contains credentials and SQL dumps)
old/

# Laravel (will be added in src/)
src/vendor/
src/node_modules/
src/.env
src/storage/*.key

# Production secrets (only template is committed)
.env.production

# Docker
db_data/
mysql_data/

# IDE
.idea/
.vscode/
*.swp
